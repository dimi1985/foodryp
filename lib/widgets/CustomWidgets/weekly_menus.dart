import 'package:flutter/material.dart';

class WeeklyMenus extends StatelessWidget {
  const WeeklyMenus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Weekly MEnus');
  }
}